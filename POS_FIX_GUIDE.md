# 🔧 แก้ไขปัญหา POS - Payment Details Columns

## ❌ อาการปัญหา
เมื่อพยายามชำระเงินในหน้า POS ขึ้น error:
```
ไม่สามารถบันทึกการขายได้: Could not find the 'cash_amount' column of 'sales_transactions' in the schema cache
```

## 🎯 สาเหตุ
ตาราง `sales_transactions` ในฐานข้อมูล Supabase ยังไม่มี columns สำหรับเก็บรายละเอียดการชำระเงินแยกตามช่องทาง:
- `cash_amount` - ยอดชำระด้วยเงินสด
- `transfer_amount` - ยอดชำระด้วยการโอนเงิน
- `card_amount` - ยอดชำระด้วยบัตรเครดิต

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: เปิด Supabase Dashboard
1. ไปที่ [supabase.com/dashboard](https://supabase.com/dashboard)
2. เลือก Project ของคุณ
3. คลิกเมนู **SQL Editor** ทางซ้าย

### ขั้นตอนที่ 2: รัน Migration Script
1. คลิก **New Query**
2. เปิดไฟล์ `supabase/add_payment_details.sql`
3. **คัดลอกโค้ดทั้งหมด** วางลงใน SQL Editor
4. คลิก **Run** (หรือกด Ctrl+Enter)
5. รอจนเห็นข้อความ ✅ **"เพิ่ม Payment Details Columns สำเร็จ!"**

### ขั้นตอนที่ 3: ตรวจสอบ
รัน SQL นี้เพื่อตรวจสอบว่า columns ถูกเพิ่มแล้ว:

```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'sales_transactions' 
  AND column_name IN ('cash_amount', 'transfer_amount', 'card_amount')
ORDER BY column_name;
```

ควรเห็นผลลัพธ์:

| column_name | data_type |
|-------------|-----------|
| card_amount | numeric |
| cash_amount | numeric |
| transfer_amount | numeric |

### ขั้นตอนที่ 4: ทดสอบ
1. **Refresh หน้าเว็บ** - กด F5 หรือ Ctrl+R
2. **ไปที่หน้า POS** - `/pos`
3. **ลองทำรายการขาย**:
   - เพิ่มสินค้าเข้าตะกร้า
   - เลือกวิธีชำระเงิน
   - กดชำระเงิน
   - ✅ ควรบันทึกสำเร็จแล้ว!

## 📋 Columns ที่เพิ่ม

### `cash_amount` (DECIMAL 12,2)
- ยอดเงินที่ชำระด้วยเงินสด
- Default: 0
- ใช้เมื่อ payment_method = 'cash' หรือ 'mixed'

### `transfer_amount` (DECIMAL 12,2)
- ยอดเงินที่ชำระด้วยการโอนเงิน
- Default: 0
- ใช้เมื่อ payment_method = 'transfer' หรือ 'mixed'

### `card_amount` (DECIMAL 12,2)
- ยอดเงินที่ชำระด้วยบัตรเครดิต
- Default: 0
- ใช้เมื่อ payment_method = 'card' หรือ 'mixed'

## 💡 ประโยชน์

การเพิ่ม columns เหล่านี้ทำให้:
1. ✅ สามารถบันทึกรายละเอียดการชำระเงินได้ครบถ้วน
2. ✅ รองรับการชำระแบบผสม (Mixed Payment)
3. ✅ ทำรายงานการขายแยกตามช่องทางชำระเงินได้
4. ✅ ตรวจสอบยอดเงินสดท้ายวันได้แม่นยำ

## 🔄 สำหรับโปรเจคใหม่

ถ้าคุณสร้างฐานข้อมูลใหม่ทั้งหมด ให้รัน:
- `supabase/schema.sql` (ที่ได้อัพเดทแล้ว)

ไฟล์นี้มี columns เหล่านี้รวมอยู่แล้ว

---

## ❓ ติดปัญหา?

หากยังแก้ไม่ได้:

1. ✅ ตรวจสอบว่ารัน SQL สำเร็จ (ไม่มี error)
2. ✅ Refresh browser แล้ว (F5)
3. ✅ ตรวจสอบ Browser Console (F12) ว่ามี error อื่นไหม
4. ✅ ตรวจสอบว่า columns ถูกเพิ่มแล้วด้วย SQL ข้างบน

---

📅 อัพเดทล่าสุด: December 2025  
💡 เอกสารเพิ่มเติม: `INSTALLATION.md`

