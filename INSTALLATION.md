# คู่มือการติดตั้งและใช้งาน
## ระบบจัดการร้านทองคำ (Thai Gold Jewelry POS & Management System)

---

## 📋 ความต้องการของระบบ (System Requirements)

- **Node.js**: เวอร์ชัน 18.0 หรือสูงกว่า
- **npm** หรือ **yarn**: สำหรับจัดการ packages
- **Supabase Account**: สำหรับ Backend และ Database
- **เบราว์เซอร์**: Chrome, Firefox, Safari, Edge (เวอร์ชันล่าสุด)

---

## 🚀 ขั้นตอนการติดตั้ง

### 1. Clone หรือ Download โปรเจค

```bash
# ถ้าใช้ Git
git clone <repository-url>
cd Saas

# หรือ Download และแตกไฟล์ ZIP
```

### 2. ติดตั้ง Dependencies

```bash
npm install
# หรือ
yarn install
```

### 3. ตั้งค่า Supabase

#### 3.1 สร้าง Supabase Project

1. ไปที่ https://supabase.com
2. สร้างบัญชีใหม่ (ถ้ายังไม่มี)
3. คลิก "New Project"
4. กรอกข้อมูล:
   - **Name**: gold-jewelry-pos
   - **Database Password**: สร้างรหัสผ่านที่แข็งแรง
   - **Region**: Southeast Asia (Singapore) - ใกล้ประเทศไทยที่สุด
5. คลิก "Create new project" และรอสักครู่

#### 3.2 รัน Database Schema

1. ในหน้า Supabase Dashboard ไปที่ **SQL Editor**
2. คลิก "New query"
3. Copy โค้ดจากไฟล์ `supabase/schema.sql`
4. Paste ลงใน SQL Editor
5. คลิก "Run" เพื่อสร้างตารางและ schema ทั้งหมด

#### 3.3 ดึง API Keys

1. ไปที่ **Settings** > **API**
2. คัดลอก:
   - **Project URL** (ตัวอย่าง: https://xxxxx.supabase.co)
   - **anon/public** key

### 4. ตั้งค่า Environment Variables

1. สร้างไฟล์ `.env.local` ที่ root ของโปรเจค:

```bash
# Windows
copy .env.example .env.local

# Mac/Linux
cp .env.example .env.local
```

2. เปิดไฟล์ `.env.local` และแก้ไขค่าต่อไปนี้:

```env
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
```

แทนที่:
- `your-supabase-project-url` ด้วย Project URL ที่คัดลอกมา
- `your-supabase-anon-key` ด้วย anon key ที่คัดลอกมา

### 5. เริ่มต้น Development Server

```bash
npm run dev
# หรือ
yarn dev
```

เปิดเบราว์เซอร์ที่ **http://localhost:3000**

---

## 📊 ตรวจสอบการติดตั้ง

### ทดสอบว่าระบบทำงานถูกต้อง:

1. ✅ เปิดหน้าแดชบอร์ดได้
2. ✅ เห็นราคาทองคำ (Mock data)
3. ✅ เข้าหน้าคลังสินค้าได้
4. ✅ เข้าหน้าจัดการลูกค้าได้
5. ✅ Sidebar และ Header แสดงผลถูกต้อง
6. ✅ ฟอนต์ภาษาไทยแสดงผลสวยงาม

---

## 🗄️ ข้อมูลเริ่มต้น (Seed Data)

ระบบมีข้อมูลเริ่มต้นที่ถูกสร้างอัตโนมัติ:

### หมวดหมู่สินค้า (Product Categories)
- แหวน (RING)
- สร้อยคอ (NECKLACE)
- กำไล (BRACELET)
- กำไลข้อมือ (BANGLE)
- ต่างหู (EARRING)
- จี้ (PENDANT)
- กำไลข้อเท้า (ANKLET)
- อื่นๆ (OTHER)

### ราคาทองตัวอย่าง (Gold Price)
- ทองคำแท่ง: รับซื้อ 38,800 / ขายออก 39,000
- ทองรูปพรรณ: รับซื้อ 38,300 / ขายออก 39,500

---

## 🎨 การใช้งานพื้นฐาน

### 1. Dashboard (หน้าแรก)
- แสดงราคาทองคำวันนี้
- สถิติยอดขาย
- รายการล่าสุด
- เมนูด่วน

### 2. ราคาทองคำวันนี้ (`/gold-prices`)
- ดูราคาทองปัจจุบัน
- ประวัติราคา 7 วันย้อนหลัง
- เครื่องมือคำนวณราคา

### 3. คลังสินค้า (`/inventory`)
- ดูรายการสินค้าทั้งหมด
- เพิ่ม/แก้ไข/ลบสินค้า
- กรองตามหมวดหมู่
- ติดตามสต๊อก

### 4. ขายสินค้า POS (`/pos`)
- เลือกสินค้าใส่ตะกร้า
- คำนวณราคา + VAT
- เทิร์นทอง
- ชำระเงิน

### 5. จัดการลูกค้า (`/customers`)
- เพิ่มข้อมูลลูกค้า
- ประวัติการซื้อ
- จัดการลูกค้า VIP

### 6. ขายฝาก/จำนำ (`/consignments`)
- สร้างสัญญาขายฝาก
- คำนวณดอกเบี้ย
- ติดตามวันครบกำหนด

### 7. ออมทอง (`/gold-savings`)
- เปิดบัญชีออมทอง
- ฝากเงิน/ล็อคน้ำหนัก
- ดูกำไร/ขาดทุน

### 8. รายงาน (`/reports`)
- รายงานยอดขาย
- สินค้าขายดี
- สถิติต่างๆ

---

## 🔧 การแก้ไขปัญหาที่พบบ่อย

### ปัญหา: ฟอนต์ภาษาไทยไม่แสดงผล
**วิธีแก้**: ตรวจสอบว่า Google Fonts โหลดสำเร็จ (ต้องมีอินเทอร์เน็ต)

### ปัญหา: ไม่สามารถเชื่อมต่อ Supabase
**วิธีแก้**: 
1. ตรวจสอบ `.env.local` ว่ามี URL และ Key ถูกต้อง
2. Restart development server (`npm run dev`)

### ปัญหา: Error "Module not found"
**วิธีแก้**: 
```bash
rm -rf node_modules
npm install
```

### ปัญหา: หน้าเว็บโหลดช้า
**วิธีแก้**: 
- ตรวจสอบความเร็วอินเทอร์เน็ต
- ลอง build production: `npm run build && npm start`

---

## 🏗️ Build สำหรับ Production

```bash
# Build
npm run build

# Start production server
npm start
```

---

## 📞 การติดต่อและการสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
- อ่านเอกสารใน `README.md`
- ตรวจสอบ Console ในเบราว์เซอร์ (F12) เพื่อดู Error messages

---

## 📝 หมายเหตุสำคัญ

⚠️ **ระบบนี้ยังอยู่ในระหว่างการพัฒนา Phase 1**

คุณสมบัติที่พร้อมใช้งาน:
- ✅ Dashboard UI
- ✅ Gold Price Display (Mock)
- ✅ Inventory Management UI
- ✅ Customer Management UI
- ✅ Consignment UI
- ✅ Gold Savings UI
- ✅ Database Schema

คุณสมบัติที่กำลังพัฒนา (Phase 2):
- ⏳ เชื่อมต่อ Supabase จริง
- ⏳ POS เต็มรูปแบบ
- ⏳ Receipt Printing
- ⏳ API Integration (สมาคมค้าทองคำ)
- ⏳ Authentication
- ⏳ Line Notify

---

**🎉 ยินดีต้อนรับสู่ระบบจัดการร้านทองคำ!**

*สร้างโดย Senior Full-stack Developer & Fintech Architect*

